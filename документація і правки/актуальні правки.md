# Актуальні правки

---

## ✅ Виконано

- **Тимчасова воронка для WayForPay — протестовано**
  - Деталі: бот Майкла (t.me/michael_fineko_bot), funnel id: 5155ca9e-1c28-4d2f-b8cf-0abc32dee2bb
  - Зроблено:
    - ✅ Посилання генерується (WayForPay API `CREATE_INVOICE`)
    - ✅ Callback від WayForPay приходить на сервер (HTTP 200), підпис збігається
    - ✅ Виправлено парсинг тіла (`parseWfpBody` — qs розбиває JSON на два ключі в місці `products:[`)
    - ✅ Юридичні сторінки створені і заповнені (ФОП Мацук О.Р., ІПН 3404907619, Тернопіль)
    - ✅ `ctx.wfp_legal_footer` автоматично встановлюється після кожного `create_invoice`
    - ✅ PDF-презентація надсилається перед оплатою через `attachmentUrl` в message-ноді
  - ⚠️ Всі тестові платежі відхилені з `"Exceed Withdrawal Frequency"` (ліміт по картці) — потрібна інша картка

- **WayForPay перенесено в обидві воронки продавців**
  - Bot Sales Automation (`3131ff8f`): консультація з автоматизації, 2500 грн
  - Bot Sales SPIN (`e7dd3dd2`): курс фінансів, 13 200 грн
  - Зроблено в обох:
    - ✅ `{{context.wfp_legal_footer}}` доданий в кінець платіжного повідомлення
    - ✅ PDF-презентація (message-нода з `attachmentUrl`) вставлена перед WayForPay-конектором
    - ✅ Виправлено ціну курсу: `$300` / `12500` → `13 200 грн` / `13200`

- **Правило: юридичний footer у всіх WayForPay повідомленнях**
  - `testSession.js` автоматично встановлює `ctx.wfp_legal_footer` при кожному `create_invoice`
  - **Правило для воронок:** будь-яке message-повідомлення після WayForPay-конектора має містити `{{context.wfp_legal_footer}}` в кінці тексту
  - Формат: `📄 Оплачуючи, ти погоджуєшся з умовами: /legal/offer | /legal/refund | /legal/terms`

- **Виправлено синтаксичну помилку в testSession.js (git `b60e3f0`)**
  - `replace(//$/,'')` → `replace(/\/$/, '')` — подвійний слеш парсився як JS-коментар, API падав при старті (500+ рестартів)

---

## 🔄 В черзі (пріоритет зверху вниз)

- **Фільтр по deep link у списку сесій воронки**
  - Задача: у списку сесій бота додати фільтр за глибоким посиланням (payload `/start payload`), по якому перейшла людина
  - Deep link зберігається в контексті сесії при старті (slug + суфікс `__l2`, `__l3` тощо)
  - Статус: Потрібно реалізувати.

- ~~**Переміщення нод у редакторі воронки** — ✅ Виконано (git `25e0b87`)~~
  - Причина: `require('@xyflow/react')` всередині callbacks не існує в браузері → drag падав з ReferenceError. Виправлено на top-level imports `applyNodeChanges`/`applyEdgeChanges`. Також прибрано `style={{cursor:'default'}}` що блокував grab-курсор.

- **Вкладка "Документи" в лівій панелі редактора**
  - Задача: додати четверту вкладку "Документи" в ліву панель (поруч з "Ноди і конектори", "Ключі", "Середовища"). Показувати документи/файли доступні для воронки (PDF та інші). Пов'язати з блоком керування файлами.
  - Статус: Потрібно реалізувати (після блоку керування файлами).

- ~~**Поле редагування файлу в налаштуваннях Message Node** — ✅ Виконано (git `1098458`)~~
  - В Message Node тепер є секція "Вкладення (PDF / файл)": показує поточний URL з кнопкою очищення, поле для URL (підтримує `{{env.VAR}}` і `{{context.var}}`), поле для назви файлу.

- ~~**Логи WayForPay у вкладці API сесії** — ✅ Виконано (git `3f256a7`)~~
  - `testSession.js`: логує CREATE_INVOICE (orderReference, amount, invoiceUrl, durationMs) з sessionId після кожного запиту до WayForPay.
  - `webhook.js`: логує вхідний callback (approved → з sessionId; підпис невалідний → без sessionId, статус 403).
  - Всі події видно у вкладці "📡 API" на сторінці перегляду сесії.

- ~~**Унікальні підписники на сторінці підписників** — ✅ Виконано (git `807d394`)~~
  - Виправлено запит: тепер включає `_count.sessions` (кількість сесій) і `project`.
  - Додано пошук за ім'ям / @username.
  - Відображається загальна кількість і діапазон пагінації (X–Y з N).
  - Посилання "Сесії →" веде на деталі користувача де вже є вкладка зі списком сесій.

- ~~**Сповіщення адміну на Telegram на початку ботів продавців** — ✅ Виконано (через ноди воронки)~~
  - В обох воронках додано `notifyAdmin` ноду одразу після `start`:
    - Bot Sales Automation: `start → notifyAdmin → Вітання → SPIN`
    - Bot Sales SPIN: `start → notifyAdmin → Вітання → SPIN`
  - Повідомлення: "🚀 Новий лід — [назва бота]\nХтось щойно запустив бота!\nЧас: {{timestamp}}"
  - Адмін отримує сповіщення в той же момент коли людина натискає /start.

- ~~**Поле Тест у сесіях** — ✅ Виконано (git `49b1413`)~~
  - `isTest Boolean` поле в Prisma Schema + міграція `006_session_is_test.sql`
  - testSession.js: `isTest: true` при створенні тестової сесії
  - API: `isTest` фільтр в GET /admin/sessions і /bots/:id/sessions; PATCH /sessions/:id/mark-test
  - Sessions.jsx: фіолетовий бейдж "тест", кнопка "Тест" в рядку, фільтр "Лише тестові"
  - ⚠️ Потрібно застосувати міграцію на сервері: `psql -U platform platform -f packages/db/migrations/006_session_is_test.sql`

- ~~**Розділ екрану перегляду сесії** — ✅ Виконано~~
  - SessionDetail.jsx: вкладка "Чат" розбита на 2 панелі
  - Ліворуч: чат-бабли (як раніше)
  - Праворуч: "Слід воронки" — для кожного повідомлення показує назву ноди (label з воронки або тип), nodeId і час
  - Наведення на рядок у правій панелі підсвічує відповідне повідомлення зліва (ring + scroll)
  - Воронка завантажується окремо (`api.getFunnel`) для отримання label нод
  - Додано кнопку "← Назад", підтримку `?back=` параметра, `isTest` бейдж у хедері

  - ~~**Видалення воронки з проекту** — ✅ Виконано~~
  - Міграція 007: `projectId` на `bots` тепер nullable (`DROP NOT NULL`)
  - Prisma schema: `projectId String?`, `project Project?`
  - API: `DELETE /projects/:id/bots/:botId` тепер встановлює `projectId: null` (замість `isActive: false`)
  - Дубль-перевірка slug при створенні: `findFirst` замість `findUnique` (старий `@@unique` прибрано)
  - Frontend: текст підтвердження пояснює що воронка залишається в системі
  - ⚠️ Потрібно застосувати міграцію: `psql -U platform platform -f packages/db/migrations/007_bot_projectid_nullable.sql`

    - **Тестування воронок в проекті сторення контенту**
  - Задача: допрацювати всі воронки так, щоб вони реально працювали
  - Статус: Потрібно реалізувати.

- ~~**Редагування воронки з POST-запитом (webhook start)** — ✅ Вже було реалізовано~~
  - StartNodeEditor: при trigger=webhook показує POST URL + кнопка копіювання
  - Поле "Схема тіла запиту" (CodeBlock) для документування параметрів
  - Поле "Webhook Note" для інструкцій

- ~~**Редагування ключів у лівій панелі — підстановка з конектора** — ✅ Виконано (git `345038b`)~~
  - В `KeyForm` додано секцію "Заповнити з конектора":
    - Dropdown вибору будь-якого збереженого конектора (не лише Claude)
    - Після вибору — кнопки для кожного поля `config` конектора + окрема кнопка "🔑 ID конектора"
    - Натискання на кнопку вставляє значення в поле "Значення"
  - `savedClaudeConnectors` тепер похідне (useMemo) від `allSavedConnectors` — один запит до API

---

## ⏳ Відкладено / Потребує додаткових даних

- **Блок керування файлами/презентаціями на платформі**
  - Задача: окремий розділ в адмінці для завантаження, перегляду та видалення файлів (PDF та інші) — щоб не класти вручну на сервер.
  - Статус: Відкладено на потім.

- **PRESENTATION_PDF_URL — файли лежать напряму на сервері**
  - PDF файли завантажено вручну: `/var/www/.../public/admin/docs/`
  - Доступні як: `https://flows.fineko.space/docs/presentation-automation.pdf`
  - і `https://flows.fineko.space/docs/presentation-course.pdf`
  - Проставлено у: тестову воронку, Bot Sales SPIN, Bot Sales Automation.
  - ⚠️ Поки немає UI для керування файлами — лише ручне завантаження по SSH.

- **Course bots 2.2, 2.3, 3.2, 4.1–4.5, 5.1–5.3 — не тестувалися**
  - Статус: Потрібне тестування після стабілізації WayForPay.

- **Мікросервіси → Content funnels — не підключено**
  - img/slides/video/render.flows.fineko.space — немає httpRequest нод.
  - Статус: Потрібно підключити після основних задач.

---

## 📋 WayForPay — вимоги до сайту (для верифікації)

Сторінки створені і заповнені на flows.fineko.space/legal/:
- ✅ /legal/contacts — контактна інформація
- ✅ /legal/refund — правила повернення
- ✅ /legal/terms — правила та умови
- ✅ /legal/offer — публічна оферта

**Реквізити заповнені:**
- ФОП: Мацук Олександр Романович
- ІПН: 3404907619
- Адреса: м. Тернопіль, вул. Львівська 1
- Телефон: +38 (096) 635-83-65
- Email: olexandrmatsuk@gmail.com

**📁 Локальні копії (синхронізовані):**
`D:\програмування\система для воронок\platform\public\admin\legal\` — у git (main)
`D:\програмування\система для воронок\документація і правки\legal-pages\` — робоча копія

**Зміни в коді:**
- git `27b7618` — `webhook.js` parseWfpBody, `testSession.js` wfp_legal_footer, legal HTML сторінки
- git `03ea992` — `testSession.js` sendDocument url + message attachmentUrl підтримка
- git `b60e3f0` — виправлено синтаксичну помилку `replace(//$/,'')` → `replace(/\/$/, '')`
